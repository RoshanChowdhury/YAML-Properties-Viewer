server:
  port: 8080
  host: localhost
  ssl:
    enabled: true
    keystore: /path/to/keystore.jks

database:
  url: jdbc:postgresql://localhost:5432/mydb
  username: admin
  password: secret123
  pool:
    size: 10
    timeout: 30000

features:
  - authentication
  - logging
  - monitoring

settings:
  debug: true
  maxConnections: 100
  timeout: 5000
